import { View, Text, TouchableOpacity, SafeAreaView } from "react-native";
import { SafeAreaProvider } from "react-native-safe-area-context";

export default function Descripcion({navigation}) {
    return(
        <SafeAreaProvider>
            <SafeAreaView>
                
            </SafeAreaView>
        </SafeAreaProvider>
    )
}